package com.bridgelabz.basicimplementation;

public class Node {
    // Defined the data and the next pointer
    int data;
    Node next;

    // constructor for initializing data and next pointing as null, as in first there will be no nodes created
    public Node(int data){
        this.data = data;
        this.next = null;
    }
}
